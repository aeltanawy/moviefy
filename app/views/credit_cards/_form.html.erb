<%= form_with(model: @credit_card, local: true) do |form| %>
  <% if @credit_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(credit_card.errors.count, "error") %> prohibited this credit_card from being saved:</h2>

      <ul>
      <% credit_card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name_on_card %>
    <%= form.text_field :name_on_card %>
  </div>

  <div class="field">
    <%= form.label "Card Number" %>
    <%= form.number_field :card_num %>
  </div>

  <div class="field">
    <%= form.label 'Expiration Date' %>
    <%= form.select :exp_month, 1..12 %>
    <%= form.select :exp_year, Date.today.year..10.years.from_now.year %>
  </div>

  <div class="field">
    <%= form.label :security_code %>
    <%= form.number_field :security_code %>
  </div>

<% end %>
