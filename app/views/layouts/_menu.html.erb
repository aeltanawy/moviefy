<nav class="navbar navbar-expand-sm fixed-top nav-dark bg-dark" role="navigation">
  <a><%= link_to('Home', root_path, :class=>"navbar-header") %></a>

  <button class="navbar-toggler navbar-dark bg-dark" type="button" data-toggle="collapse" data-target="#navbarToggler" aria-controls="navbarToggler" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarToggler">
    <ul class="navbar-nav ml-auto">
      <li class="nav-item" style="margin-top:8px;">
        <a><%= link_to "Find a Theater", theater_map_path %> </a>
      </li>
      <li class="nav-item" style="margin-top:8px;">
        <a><%= link_to "Movies", movies_path %> </a>
      </li>
      <li class="nav-item" style="margin-top:8px;">
        <a><%= link_to 'Crews', crews_path %></a>
      </li>
      <li class="nav-item" style="margin-top:8px;">
        <a><%= link_to "Search" %> </a>
      </li>
      <li class="nav-item" style="margin-top:8px; margin-left: 12px; color:white"> | </li>

      <% if logged_in? %>
        <!-- if a user is logged in -->
        <li class="nav-item" style="margin-top:8px;">
          <a><%= link_to "Log out", logout_path %> </a>
        </li>
        <!-- add admin label if admin -->
        <% if admin? %>
          <li class="nav-item" style="margin-top:8px;">
            <a><%= link_to 'Admin', admin_dashboard_path %></a>
          </li>
        <% else %>
          <li class="nav-item" style="margin-top:8px;">
            <a><%= link_to 'Profile', user_path(session[:user_id]) %></a>
          </li>
        <% end %>
      <% else %>
        <!-- if no user is logged in -->
        <li class="nav-item" style="margin-top:8px;">
          <a><%= link_to "Log in / Sign up", login_path %> </a>
        </li>
      <% end %>

    </ul>
  </div>
</nav>
